#if storiesNumber <= 5 - stories don't move on screen, 
#if > 5 - all stories move down or up after elevator get 3rd+ story   
storiesNumber=7
elevatorCapacity=5
passengersNumber=27
#animationBoots can be [0-100], because boost=100ms/animationBoots 
#if more it will be working, but doesn't changes boost cardinally
#if animationBoots is value of 0 - swing app doesn't start 
animationBoots=50